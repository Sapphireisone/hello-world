name: Say-hello
on: push
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v3
            - name: Install deps
              run: poetry install
            - name: Build
              run: poetry build
            - name: Say-hello
              run: poetry run say-hello
